# ---------------------------------------------------------------------------
# Heavy ML stack (torch + transformers via sentence-transformers).
# Installed only where embeddings are actually computed: the daily pipeline
# job and any worker that ranks jobs. Keeps the API image small.
#
#   pip install -r requirements.txt -r requirements-ml.txt
# ---------------------------------------------------------------------------
-r requirements.txt
# Verified-compatible modern stack (see pyproject [ml] for rationale).
# transformers 5.x requires huggingface-hub>=1.3 — pin it explicitly so a stale
# 0.25.x (which dropped `is_offline_mode`) is never left in place on upgrade.
sentence-transformers>=5.0,<6.0
transformers>=5.0,<6.0
huggingface-hub>=1.3,<2.0
